<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ttn.part_3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewpager.MotionLayoutWithViewPager">

        </activity>
        <activity android:name=".appdrawer.MotionLayoutWithDrawerLayout">

        </activity>
        <activity android:name=".collapsible_toolbar.MotionLayoutWithCoordinatorLayout"></activity>
        <activity android:name=".KeyFrameSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>